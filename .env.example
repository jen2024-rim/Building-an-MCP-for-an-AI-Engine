# This project currently uses only local JSON fixtures in /data and
# does not require any API keys or secrets to run.
#
# If a future external API integration needs credentials, add the
# placeholder name here (never a real value) and copy this file to
# .env locally — .env is gitignored and must never be committed.
#
# EXAMPLE_API_KEY=
